# Temporary updater check

Temporary v0.1.15 release used only to validate the v0.1.14 startup self-update flow.

Expected post-update visual marker: main window title and top-left product label show `태영 헬퍼`.

Delete the temporary release/tag/branch after the user confirms the update worked.
