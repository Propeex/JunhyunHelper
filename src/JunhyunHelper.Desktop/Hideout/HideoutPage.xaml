<UserControl x:Class="JunhyunHelper.Desktop.Hideout.HideoutPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BackgroundDarkBrush}">
    <UserControl.Resources>
        <DataTemplate x:Key="StationRowTemplate">
            <Border x:Name="RowBorder"
                    Padding="12,10"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="0,0,0,1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="{Binding Name}"
                               FontWeight="SemiBold"
                               VerticalAlignment="Center" />
                    <TextBlock Grid.Column="1"
                               Text="{Binding LevelText}"
                               Foreground="{StaticResource AccentBrush}"
                               VerticalAlignment="Center"
                               Margin="12,0,0,0" />
                </Grid>
            </Border>
            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="True">
                    <Setter TargetName="RowBorder" Property="Background" Value="{StaticResource BackgroundLightBrush}" />
                </DataTrigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="RowBorder" Property="Background" Value="{StaticResource BackgroundMediumBrush}" />
                </Trigger>
            </DataTemplate.Triggers>
        </DataTemplate>
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBox x:Name="SearchBox"
                 Grid.Row="0"
                 Margin="0,0,0,10"
                 ToolTip="은신처 시설 검색"
                 TextChanged="SearchBox_TextChanged" />

        <TextBlock x:Name="SummaryText"
                   Grid.Row="1"
                   Foreground="{StaticResource TextSecondaryBrush}"
                   Margin="2,0,0,10" />

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MinWidth="340" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="420" MinWidth="320" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Background="{StaticResource BackgroundMediumBrush}"
                    CornerRadius="6">
                <ListBox x:Name="StationList"
                         ItemTemplate="{StaticResource StationRowTemplate}"
                         SelectionChanged="StationList_SelectionChanged"
                         VirtualizingPanel.IsVirtualizing="True"
                         VirtualizingPanel.VirtualizationMode="Recycling" />
            </Border>

            <GridSplitter Grid.Column="1" Width="8" Background="Transparent" />

            <Border Grid.Column="2"
                    Background="{StaticResource BackgroundMediumBrush}"
                    CornerRadius="6"
                    Padding="16">
                <Grid>
                    <TextBlock x:Name="EmptyDetailText"
                               Text="시설을 선택하면 현재 레벨과 다음 업그레이드 정보를 확인할 수 있습니다."
                               Foreground="{StaticResource TextSecondaryBrush}"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               TextAlignment="Center" />

                    <ScrollViewer x:Name="DetailScroll"
                                  Visibility="Collapsed"
                                  VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <TextBlock x:Name="DetailName"
                                       FontSize="22"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource AccentBrush}"
                                       Margin="0,0,0,14" />

                            <TextBlock Text="현재 레벨"
                                       FontWeight="SemiBold"
                                       Margin="0,0,0,6" />
                            <ComboBox x:Name="LevelComboBox"
                                      Margin="0,0,0,18"
                                      SelectionChanged="LevelComboBox_SelectionChanged" />

                            <TextBlock x:Name="NextLevelHeader"
                                       FontWeight="SemiBold"
                                       Margin="0,0,0,6" />
                            <Border Background="{StaticResource BackgroundDarkBrush}"
                                    CornerRadius="5"
                                    Padding="10"
                                    Margin="0,0,0,14">
                                <StackPanel>
                                    <TextBlock x:Name="NextLevelMessage"
                                               Foreground="{StaticResource TextSecondaryBrush}"
                                               Margin="0,0,0,8" />
                                    <ItemsControl x:Name="NextLevelItems" />
                                </StackPanel>
                            </Border>

                            <TextBlock x:Name="ConstructionTimeText"
                                       Foreground="{StaticResource TextSecondaryBrush}" />
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
