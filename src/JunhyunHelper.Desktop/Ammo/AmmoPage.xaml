<UserControl x:Class="JunhyunHelper.Desktop.Ammo.AmmoPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BackgroundDarkBrush}">
    <UserControl.Resources>
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="{StaticResource BackgroundMediumBrush}" />
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="GridLinesVisibility" Value="Horizontal" />
            <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="VerticalGridLinesBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="RowBackground" Value="{StaticResource BackgroundMediumBrush}" />
            <Setter Property="AlternatingRowBackground" Value="{StaticResource BackgroundDarkBrush}" />
            <Setter Property="HeadersVisibility" Value="Column" />
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{StaticResource BackgroundLightBrush}" />
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="Padding" Value="8,7" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="7,6" />
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
        </Style>
        <Style TargetType="DataGridRow">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource BackgroundLightBrush}" />
                    <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="3*" />
            <RowDefinition Height="8" />
            <RowDefinition Height="2*" MinHeight="190" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="220" />
            </Grid.ColumnDefinitions>

            <TextBox x:Name="SearchBox"
                     Grid.Column="0"
                     Margin="0,0,12,0"
                     ToolTip="탄약 이름 검색"
                     TextChanged="SearchBox_TextChanged" />

            <TextBlock Grid.Column="1"
                       Text="구경"
                       VerticalAlignment="Center"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       Margin="0,0,8,0" />

            <ComboBox x:Name="CaliberComboBox"
                      Grid.Column="2"
                      SelectionChanged="CaliberComboBox_SelectionChanged" />
        </Grid>

        <TextBlock x:Name="SummaryText"
                   Grid.Row="1"
                   Foreground="{StaticResource TextSecondaryBrush}"
                   Margin="2,0,0,10" />

        <DataGrid x:Name="AmmoGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserReorderColumns="True"
                  CanUserResizeColumns="True"
                  CanUserSortColumns="True"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  AlternationCount="2"
                  SelectionChanged="AmmoGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="탄약" Binding="{Binding Name}" Width="2*" MinWidth="180" />
                <DataGridTextColumn Header="구경" Binding="{Binding CaliberLabel}" Width="140" />
                <DataGridTextColumn Header="피해" Binding="{Binding DamageText}" Width="85" />
                <DataGridTextColumn Header="관통" Binding="{Binding PenetrationPower}" Width="70" />
                <DataGridTextColumn Header="장갑 피해" Binding="{Binding ArmorDamageText}" Width="90" />
                <DataGridTextColumn Header="속도" Binding="{Binding SpeedText}" Width="95" />
                <DataGridTextColumn Header="파편" Binding="{Binding FragmentationText}" Width="75" />
                <DataGridTextColumn Header="반동" Binding="{Binding RecoilText}" Width="75" />
                <DataGridTextColumn Header="명중" Binding="{Binding AccuracyText}" Width="75" />
                <DataGridTextColumn Header="수급" Binding="{Binding AcquisitionCountText}" Width="80" />
            </DataGrid.Columns>
        </DataGrid>

        <GridSplitter Grid.Row="3"
                      Height="8"
                      Background="Transparent"
                      HorizontalAlignment="Stretch" />

        <Border Grid.Row="4"
                Background="{StaticResource BackgroundMediumBrush}"
                CornerRadius="6"
                Padding="16">
            <Grid>
                <TextBlock x:Name="EmptyDetailText"
                           Text="탄약을 선택하면 세부 성능과 수급처를 확인할 수 있습니다."
                           Foreground="{StaticResource TextSecondaryBrush}"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center" />

                <Grid x:Name="DetailGrid" Visibility="Collapsed">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="360" MinWidth="300" />
                        <ColumnDefinition Width="16" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <TextBlock x:Name="DetailName"
                                       FontSize="21"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource AccentBrush}"
                                       Margin="0,0,0,10" />
                            <TextBlock x:Name="DetailIdentity"
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       Margin="0,0,0,12" />
                            <TextBlock x:Name="DetailPerformance" />
                            <TextBlock x:Name="DetailModifiers"
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       Margin="0,8,0,0" />
                        </StackPanel>
                    </ScrollViewer>

                    <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <TextBlock Text="수급처"
                                       FontSize="16"
                                       FontWeight="SemiBold"
                                       Margin="0,0,0,8" />
                            <ItemsControl x:Name="AcquisitionItems">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="{StaticResource BackgroundDarkBrush}"
                                                BorderBrush="{StaticResource BorderBrush}"
                                                BorderThickness="1"
                                                CornerRadius="5"
                                                Padding="10"
                                                Margin="0,0,0,8">
                                            <StackPanel>
                                                <TextBlock Text="{Binding Title}"
                                                           FontWeight="SemiBold"
                                                           Foreground="{StaticResource AccentBrush}" />
                                                <TextBlock Text="{Binding Conditions}"
                                                           Foreground="{StaticResource TextSecondaryBrush}"
                                                           Margin="0,3,0,0" />
                                                <TextBlock Text="{Binding Details}"
                                                           Margin="0,3,0,0" />
                                                <TextBlock Text="{Binding Unlock}"
                                                           Foreground="{StaticResource WarningBrush}"
                                                           Margin="0,3,0,0" />
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
