<UserControl x:Class="JunhyunHelper.Desktop.FarmingGuide.FarmingGuidePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Focusable="True"
             PreviewMouseMove="Root_PreviewMouseMove"
             PreviewMouseLeftButtonUp="Root_PreviewMouseLeftButtonUp"
             PreviewKeyDown="Root_PreviewKeyDown">
    <Grid x:Name="RootGrid" Background="{StaticResource BackgroundDarkBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="390" />
            <ColumnDefinition Width="*" MinWidth="400" />
            <ColumnDefinition Width="350" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,1,0" Padding="14">
            <DockPanel LastChildFill="True">
                <StackPanel DockPanel.Dock="Top">
                    <TextBlock Text="장비" FontSize="21" FontWeight="SemiBold" Margin="0,0,0,10" />
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <ComboBox x:Name="PresetComboBox"
                                  Grid.Column="0"
                                  MinHeight="36"
                                  VerticalContentAlignment="Center"
                                  SelectionChanged="PresetComboBox_SelectionChanged" />
                        <Button Grid.Column="1"
                                Width="40"
                                Height="36"
                                Margin="7,0,0,0"
                                Padding="8"
                                ToolTip="현재 상태를 새 프리셋으로 저장"
                                Click="SavePresetButton_Click">
                            <Viewbox Width="18" Height="18" Stretch="Uniform">
                                <Grid Width="18" Height="18">
                                    <Border BorderBrush="{StaticResource TextSecondaryBrush}"
                                            BorderThickness="1.5"
                                            CornerRadius="1" />
                                    <Border Width="9" Height="5"
                                            Margin="0,2,0,0"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Top"
                                            BorderBrush="{StaticResource TextSecondaryBrush}"
                                            BorderThickness="1.2" />
                                    <Border Width="10" Height="6"
                                            Margin="0,0,0,2"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Bottom"
                                            BorderBrush="{StaticResource TextSecondaryBrush}"
                                            BorderThickness="1.2" />
                                </Grid>
                            </Viewbox>
                        </Button>
                        <Button x:Name="DeletePresetButton"
                                Grid.Column="2"
                                Width="40"
                                Height="36"
                                Margin="7,0,0,0"
                                Padding="8"
                                IsEnabled="False"
                                ToolTip="선택한 프리셋 삭제"
                                Click="DeletePresetButton_Click">
                            <Viewbox Width="18" Height="18" Stretch="Uniform">
                                <Canvas Width="18" Height="18">
                                    <Path Stroke="{StaticResource TextSecondaryBrush}"
                                          StrokeThickness="1.5"
                                          StrokeStartLineCap="Round"
                                          StrokeEndLineCap="Round"
                                          StrokeLineJoin="Round"
                                          Data="M 4,5 L 14,5 M 7,3 L 11,3 L 12,5 M 5.5,5 L 6.2,15 L 11.8,15 L 12.5,5 M 8,8 L 8.3,12.5 M 10,8 L 9.7,12.5" />
                                </Canvas>
                            </Viewbox>
                        </Button>
                    </Grid>
                </StackPanel>

                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <StackPanel x:Name="EquipmentPanel" />
                </ScrollViewer>
            </DockPanel>
        </Border>

        <Border Grid.Column="1" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,1,0" Padding="14">
            <DockPanel LastChildFill="True">
                <StackPanel DockPanel.Dock="Top" Margin="0,0,0,10">
                    <TextBlock Text="수납 공간" FontSize="21" FontWeight="SemiBold" />
                    <TextBlock Text="아이템을 끌어 놓으세요. 드래그 중 R로 회전할 수 있습니다."
                               Foreground="{StaticResource TextSecondaryBrush}"
                               Margin="0,3,0,0" />
                </StackPanel>
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <StackPanel x:Name="StoragePanel" />
                </ScrollViewer>
            </DockPanel>
        </Border>

        <Grid Grid.Column="2" Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBox x:Name="SearchTextBox"
                     Grid.Row="0"
                     MinHeight="40"
                     Padding="10,2"
                     VerticalContentAlignment="Center"
                     TextChanged="SearchTextBox_TextChanged"
                     ToolTip="한글/영문 이름으로 아이템 검색" />

            <Border Grid.Row="1"
                    Margin="0,10,0,12"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="7"
                    Background="{StaticResource BackgroundMediumBrush}">
                <ListBox x:Name="SearchResultsList"
                         BorderThickness="0"
                         Background="Transparent"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="7"
                                    Margin="2"
                                    CornerRadius="5"
                                    Background="{StaticResource BackgroundDarkBrush}"
                                    Cursor="Hand"
                                    Tag="{Binding}"
                                    MouseLeftButtonDown="SearchResult_MouseLeftButtonDown">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="54" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <Border Width="48" Height="48"
                                            BorderBrush="{StaticResource BorderBrush}"
                                            BorderThickness="1"
                                            CornerRadius="4"
                                            Background="{StaticResource BackgroundMediumBrush}">
                                        <Image Source="{Binding Image}" Stretch="Uniform" Margin="2" />
                                    </Border>
                                    <StackPanel Grid.Column="1" Margin="8,0,6,0" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                                        <TextBlock Text="{Binding EnglishName}"
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   FontSize="11"
                                                   TextTrimming="CharacterEllipsis" />
                                    </StackPanel>
                                    <TextBlock Grid.Column="2"
                                               Text="{Binding SizeText}"
                                               Foreground="{StaticResource AccentBrush}"
                                               VerticalAlignment="Center" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>

            <Border Grid.Row="2"
                    Padding="14"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="7"
                    Background="{StaticResource BackgroundMediumBrush}">
                <StackPanel>
                    <TextBlock Text="현재 상태" FontSize="17" FontWeight="SemiBold" Margin="0,0,0,10" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="파밍한 가치" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,2" />
                        <TextBlock x:Name="ValueSummaryText" Grid.Row="0" Grid.Column="1" Text="—" FontWeight="SemiBold" Margin="0,2" />
                        <TextBlock Grid.Row="1" Text="총 무게" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,2" />
                        <TextBlock x:Name="WeightSummaryText" Grid.Row="1" Grid.Column="1" Text="0.00 kg" FontWeight="SemiBold" Margin="0,2" />
                        <TextBlock Grid.Row="2" Text="수납 공간" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,2" />
                        <TextBlock x:Name="StorageSummaryText" Grid.Row="2" Grid.Column="1" Text="0 / 0" FontWeight="SemiBold" Margin="0,2" />
                    </Grid>
                </StackPanel>
            </Border>
        </Grid>

        <Border x:Name="WorkbenchHost"
                Grid.Column="1"
                Margin="12"
                Padding="14"
                Visibility="Collapsed"
                Panel.ZIndex="40"
                BorderBrush="{StaticResource AccentBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Background="{StaticResource BackgroundDarkBrush}">
            <DockPanel LastChildFill="True">
                <Grid DockPanel.Dock="Top" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="WorkbenchTitleText"
                               FontSize="19"
                               FontWeight="SemiBold"
                               VerticalAlignment="Center"
                               TextTrimming="CharacterEllipsis" />
                    <Button Grid.Column="1"
                            Width="34"
                            Height="34"
                            Margin="10,0,0,0"
                            Padding="0"
                            Content="×"
                            FontSize="18"
                            ToolTip="내부 보기 닫기"
                            Click="CloseWorkbenchButton_Click" />
                </Grid>
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <StackPanel x:Name="WorkbenchPanel" />
                </ScrollViewer>
            </DockPanel>
        </Border>

        <Canvas x:Name="DragOverlay"
                Grid.ColumnSpan="3"
                Panel.ZIndex="100"
                IsHitTestVisible="False" />
    </Grid>
</UserControl>