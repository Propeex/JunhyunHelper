# AGENTS.md — 준현 헬퍼 개발자 작업 규약

이 파일은 새 대화, 새 세션, 새 개발자가 이 저장소를 이어서 작업할 때 따라야 하는 최상위 개발 규약입니다.

## 1. 시작 시 필수 복구 절차

작업을 시작하기 전에 반드시 아래 순서로 읽습니다.

1. `README.md`
2. `docs/STATE.md`
3. `docs/PRODUCT.md`
4. `docs/DECISIONS.md`
5. `docs/ARCHITECTURE.md`
6. `docs/DEVELOPMENT.md`
7. `docs/REFERENCE_POLICY.md`
8. 현재 작업과 관련된 코드/테스트/이슈/PR

문서와 코드가 충돌하면 임의로 추측하지 말고 **사용자 의도와 확정 기록을 우선하여 원인을 조사**합니다.

## 2. 진실의 우선순위

높은 순서부터 적용합니다.

1. 사용자가 현재 대화에서 명시적으로 확정한 의도/요구사항
2. `docs/DECISIONS.md`의 유효한 확정 결정
3. `docs/PRODUCT.md`의 확정 요구사항
4. `docs/ARCHITECTURE.md`의 현재 기술 설계
5. 테스트가 명시하는 현재 계약
6. 현재 코드의 동작
7. 기존 `Propeex/Tarkov-Helper`, RatScanner 등 참고 구현

**기존 구현이 현재 새 제품의 요구사항을 결정해서는 안 됩니다.**

## 3. 확정과 가설을 분리

모든 중요한 내용은 다음 상태 중 하나로 취급합니다.

- `INTENT_CAPTURED` — 사용자가 구두로 전달한 원래 의도는 기록했지만 개발자의 해석과 세부 동작은 아직 검증되지 않음
- `CONFIRMED` — 사용자와 해석을 맞추고 실제 동작까지 확정했거나 이미 공식 결정으로 기록됨
- `PROPOSED` — 개발자가 제안했으나 아직 확정되지 않음
- `OPEN` — 질문/검토가 필요한 미결 사항
- `SUPERSEDED` — 이후 결정으로 폐기된 과거 결정

개발자가 자신의 추론을 사용자 결정처럼 기록하면 안 됩니다.

## 4. 구두 의도 정렬 절차

사용자가 기능이나 아이디어를 구두로 설명했다고 해서 곧바로 완성된 요구사항으로 변환하지 않습니다.

새 기능/제품 방향은 기본적으로 다음 순서를 따릅니다.

1. **원래 의도 기록** — 사용자가 직접 말한 내용과 범위를 과도한 해석 없이 `INTENT_CAPTURED`로 보존합니다.
2. **개발자 해석 설명** — 개발자가 그 말을 어떤 제품 개념으로 이해했는지 사용자에게 다시 설명합니다.
3. **의도 교정** — 사용자가 잘못 이해된 부분, 빠진 부분, 중요도 차이를 교정합니다.
4. **작동 방식 협의** — 실제 화면/사용 상황에서 무엇을 보고, 무엇을 누르고, 프로그램이 무엇을 계산·표시·저장하는지 기능별로 논의합니다.
5. **요구사항 확정** — 사용자와 의미와 동작이 맞았을 때만 `CONFIRMED` 요구사항과 Flow로 기록합니다.

따라서 기능 이름이나 짧은 설명만으로 다음을 임의 확정하지 않습니다.

- 정확한 UI 구조
- 필터/정렬/자동 계산 규칙
- 기능 간 자동 연동
- 데이터 저장 방식
- 예외 처리
- 사용자가 기대하는 정보 밀도와 조작 방식

질문은 구현자가 코드에서 확인할 수 있는 내용이 아니라 **제품 의도에 따라 결과가 달라지는 지점**에 집중합니다.

## 5. 설계와 구현의 관계

준현 헬퍼의 핵심 v0.1 기능은 이미 구현되어 있으며 현재는 release/maintenance 단계입니다.

따라서 다음을 구분합니다.

### 이미 확정된 기능의 버그 수정 / 회귀 수정 / 성능 개선 / 릴리즈 하드닝

- 사용자에게 개발 절차를 다시 묻지 않고 현재 요구사항/테스트/코드를 조사해 바로 수정할 수 있습니다.
- 기존 확정 동작을 바꾸지 않는 범위에서는 불필요한 재설계를 하지 않습니다.
- 변경 후 관련 회귀 테스트와 실제 제품 경로를 검증합니다.

### 새 기능 / 제품 의미가 바뀌는 변경

구현 전 최소한 다음이 정의되어야 합니다.

- 해결하려는 사용자 문제가 명확함
- 사용 흐름과 기대 동작이 명확함
- 입력/출력/실패 동작이 정의됨
- 데이터 출처와 신뢰 기준이 정의됨
- 사용자가 기능 방향을 확정함
- 검증 방법이 정의됨

제품 의도가 불명확한 새 기능을 기존 코드의 동작만 보고 임의 구현하지 않습니다.

## 6. 기능 추적성

중요 기능은 가능하면 다음 연결이 끊기지 않게 유지합니다.

`사용자 원래 의도 → 합의된 해석 → 제품 요구사항 → 동작 규칙 → 데이터/외부 의존성 → 구현 구성요소 → 검증/테스트`

무엇을 구현했는지는 물론 **왜 존재하는 기능인지**까지 저장소만 보고 설명할 수 있어야 합니다.

## 7. 대화 종료/작업 종료 시 인수인계

의미 있는 작업이 끝날 때 반드시 필요한 문서를 갱신합니다.

- `docs/STATE.md`: 현재 상태, 이번에 바뀐 것, 다음 행동, 막힌 점
- `docs/PRODUCT.md`: 새로 확정/변경된 제품 요구사항 및 아직 정렬 중인 사용자 의도
- `docs/DECISIONS.md`: 장기적으로 의미 있는 확정 결정
- `docs/ARCHITECTURE.md`: 기술 구조가 실제로 바뀐 경우

단순 대화 요약을 남기는 것이 목적이 아닙니다. **다음 개발자가 저장소만 읽고 정확히 이어갈 수 있는 상태**를 만드는 것이 목적입니다.

## 8. 기존 Tarkov-Helper 취급

기존 `Propeex/Tarkov-Helper`는 다음 용도로만 봅니다.

- 과거에 어떤 문제를 해결하려 했는지 조사
- 재사용 가능한 자산/아이디어 후보 탐색
- 이미 겪었던 기술적 함정 파악
- 새 설계와 비교 검증

다음은 금지합니다.

- 기존 동작을 이유 없이 새 사양으로 채택
- 기존 코드의 가정이나 게임 정보를 사실로 간주
- 기존 구조에 새 제품 설계를 억지로 맞춤
- 오래된 데이터/로직을 검증 없이 복사

Map/MiniMap은 사용자가 검증한 특정 Tarkov-Helper revision을 **명시적으로 제품 기준선으로 채택한 예외적 subsystem**입니다. 이 경우에도 JunhyunHelper 제품 요구사항이 우선하며, old Tarkov-Helper의 비-Map 기능이나 updater가 자동으로 제품 사양이 되는 것은 아닙니다.

세부 규칙은 `docs/REFERENCE_POLICY.md`와 `docs/MAP_PRODUCT_REQUIREMENTS.md`를 따릅니다.

## 9. 사용자와 개발자의 역할

사용자는 **의도, 우선순위, 사용 경험, 피드백, 최종 판단**을 제공합니다.

개발자는 다음을 책임집니다.

- 사용자의 구두 의도를 정확한 제품 개념으로 해석하고 다시 확인
- 요구사항 구조화
- 모순/누락 탐지
- 제품 및 기술 설계
- 구현
- 검증
- 저장소 상태 관리
- 다음 대화로 이어질 프로젝트 기억 유지
- release packaging / CI / GitHub 운영

사용자에게 Git, 브랜치, PR, CI 등의 세부 개발 절차를 관리 책임으로 넘기지 않습니다. 필요한 개발 절차는 개발자가 내부적으로 처리하고, 사용자에게는 제품 경험과 결과에 영향을 주는 판단만 명확하게 설명합니다.

## 10. 릴리즈 원칙

릴리즈는 "빌드가 된다"만으로 완료하지 않습니다.

모든 릴리즈 후보는 **GitHub 릴리즈 직전에 변경 범위 전체를 다시 최종 검토**합니다. 이 검토에서 기능 회귀, 미해결 review 지적, 성능 위험, 패키징 문제, 문서/실제 동작 불일치가 발견되면 릴리즈하지 않고 먼저 수정한 뒤 release gate를 처음부터 다시 통과시킵니다. 최종 검토와 release gate에 문제가 없으면 사용자의 별도 Git/PR/CI 확인을 요구하지 않고 개발자가 병합, 버전 확정, GitHub Release 및 배포물 게시까지 완료합니다.

최소한 다음을 확인합니다.

- 사용자 검증이 필요한 핵심 흐름의 실제 동작
- 자동 테스트 / Windows publish / 실제 startup smoke
- 정상 종료 후 process가 남지 않는지
- 사용자 데이터와 Game Content update의 분리/보존
- 배포물에 debug symbol/임시 파일/불필요 dependency가 없는지
- 배포 안내문과 공식 문서가 실제 기능과 일치하는지
- 미구현 기능을 완성된 제품 surface처럼 노출하지 않는지
- PR/review에서 제기된 release-blocking 지적이 모두 해결되었는지
- 최종 배포 ZIP의 root layout, 실행 파일, PDB/DLL/nested ZIP/Logs 오염 여부와 SHA-256

현재 구체적인 release gate는 `docs/STATE.md`를 따릅니다.
