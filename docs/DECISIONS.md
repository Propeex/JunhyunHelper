# DECISIONS — 확정 결정 기록

이 문서는 제품과 개발 방향에 장기적으로 영향을 주는 **확정 결정과 그 이유**를 보존합니다.

과거 결정을 삭제해서 역사를 지우지 않습니다. 변경되면 기존 항목을 `SUPERSEDED`로 표시하고 새 결정을 추가합니다.

## 기록 형식

### DEC-XXX — 제목

- 상태: `CONFIRMED | SUPERSEDED`
- 날짜:
- 결정:
- 이유:
- 대안/배경:
- 영향:
- 대체한 결정:

---

## DEC-001 — 새 제품은 처음부터 설계한다

- 상태: `CONFIRMED`
- 날짜: 2026-08-08
- 결정: 새로운 Tarkov Helper의 제품 사양을 기존 구현에서 승계하지 않고 사용자와의 대화를 통해 처음부터 정의한다.
- 이유: 기존 Tarkov-Helper는 불완전하며 잘못된 정보나 잘못된 설계 가정을 포함할 수 있다.
- 대안/배경: 기존 코드를 기준으로 리팩터링하는 방식은 채택하지 않았다.
- 영향: 제품 요구사항은 사용자 의도와 새로 검증한 사실을 기준으로 만들어야 한다.
- 대체한 결정: 없음

## DEC-002 — 기존 Tarkov-Helper는 참고 자료로만 취급한다

- 상태: `CONFIRMED`
- 날짜: 2026-08-08
- 결정: `Propeex/Tarkov-Helper`는 필요할 때 조사할 수 있지만 제품 사양 또는 사실의 최상위 근거로 사용하지 않는다.
- 이유: 기존 구현을 무비판적으로 계승하면 오류와 오래된 가정도 함께 계승할 수 있다.
- 대안/배경: 기존 구현의 동작을 새 제품의 기본값으로 삼는 방식은 배제한다.
- 영향: 기존 코드 재사용 전에는 새 요구사항과의 적합성 및 정보 정확성을 별도로 검증한다.
- 대체한 결정: 없음

## DEC-003 — GitHub 저장소를 프로젝트 기억의 공식 기반으로 사용한다

- 상태: `CONFIRMED`
- 날짜: 2026-08-08
- 결정: 대화가 바뀌어도 개발 연속성을 유지하기 위해 제품 사양, 결정, 현재 상태, 기술 설계를 GitHub 저장소에 기록한다.
- 이유: 개별 대화의 문맥에만 의존하면 장기 개발 과정에서 결정 이유와 현재 상태가 유실될 수 있다.
- 대안/배경: 대화 요약만으로 인수인계하는 방식은 충분하지 않다고 판단했다.
- 영향: 의미 있는 작업이 끝날 때 공식 문서를 함께 갱신해야 한다.
- 대체한 결정: 없음

## DEC-004 — 사용자는 제품 판단에 집중하고 개발 절차는 개발자가 책임진다

- 상태: `CONFIRMED`
- 날짜: 2026-08-08
- 결정: 사용자는 의도와 피드백을 제공하고, Git/브랜치/PR/CI/구현 세부사항 등 개발 운영은 개발자가 담당한다.
- 이유: 사용자가 개발자가 아니어도 제품 개발을 진행할 수 있어야 한다.
- 대안/배경: 개발 절차를 사용자가 직접 관리하는 운영 방식은 목표가 아니다.
- 영향: 사용자에게는 제품 경험과 결과에 영향을 주는 판단을 중심으로 설명한다.
- 대체한 결정: 없음

## DEC-005 — 제품 설계가 구현보다 선행한다

- 상태: `CONFIRMED`
- 날짜: 2026-08-08
- 결정: 현재 단계에서는 제품 코드를 작성하지 않고, 핵심 사용 상황과 요구사항을 먼저 정의한다.
- 이유: 구현을 먼저 시작하면 기술 구조가 제품 방향을 역으로 제한할 수 있다.
- 대안/배경: 기존 코드부터 복사하거나 새 UI/기능을 즉시 구현하는 방식은 보류한다.
- 영향: 기술 스택과 아키텍처도 제품 요구가 충분히 드러난 뒤 확정한다.
- 대체한 결정: 없음

## DEC-006 — 공식 제품명은 준현 헬퍼로 한다

- 상태: `CONFIRMED`
- 날짜: 2026-08-08
- 결정: 사용자에게 보이는 제품의 공식 이름은 **준현 헬퍼**로 한다.
- 이유: 사용자가 제품명을 직접 지정했다.
- 대안/배경: 저장소 이름 `JunhyunHelper`는 개발 저장소 식별자로 유지할 수 있다.
- 영향: 제품 문서와 향후 UI에서는 별도 결정이 없는 한 `준현 헬퍼`를 사용한다.
- 대체한 결정: 없음

## DEC-007 — 초기 상위 기능 범위를 6개 영역으로 정의한다

- 상태: `CONFIRMED`
- 날짜: 2026-08-08
- 결정: 준현 헬퍼의 초기 제품 논의 영역을 퀘스트 목록, 은신처 목록, 필요 아이템, 탄약 표, 지도, RatScanner 통합으로 둔다. RatScanner 통합은 후속 추가 대상으로 둔다.
- 이유: 사용자가 준현 헬퍼에서 다루고 싶은 기능 영역을 직접 제시했다.
- 대안/배경: 이 결정은 각 기능의 구체적인 의미나 작동 방식까지 확정했다는 뜻이 아니다.
- 영향: 제품 설계는 이 기능 영역을 중심으로 대화하되, 기능별 상세 요구사항은 별도의 의도 정렬 과정을 거쳐야 한다.
- 대체한 결정: 없음

## DEC-008 — 구두 의도는 해석을 맞춘 뒤 요구사항으로 확정한다

- 상태: `CONFIRMED`
- 날짜: 2026-08-08
- 결정: 사용자가 구두로 전달한 기능 설명은 `INTENT_CAPTURED` 상태로 기록하고, 개발자가 이해한 내용을 사용자에게 다시 설명하여 교정받은 뒤 실제 작동 방식을 협의한다. 의미와 동작이 맞은 내용만 `CONFIRMED` 제품 요구사항으로 기록한다.
- 이유: 짧은 구두 설명만으로는 개발자가 사용자의 실제 의도, 사용 맥락, 기대 동작을 잘못 해석할 수 있으며, 기능 이름이 같더라도 원하는 제품 경험이 다를 수 있다.
- 대안/배경: 초기에는 기능 목록을 받은 즉시 상세 요구사항으로 변환했으나, 사용자는 먼저 서로 의도를 맞추고 각 기능의 작동 방식을 대화해야 한다고 명확히 지적했다.
- 영향: 향후 제품 설계는 `원래 의도 기록 → 개발자 해석 설명 → 사용자 교정 → 작동 방식 협의 → 공식 요구사항 확정` 순서를 기본 절차로 사용한다. 개발자는 자동 연동, UI, 계산 규칙 등을 임의로 앞서 확정하지 않는다.
- 대체한 결정: 없음

## DEC-009 — 퀘스트 원천 데이터는 json.tarkov.dev에서 받고 내부 모델로 변환한다

- 상태: `CONFIRMED`
- 날짜: 2026-08-08
- 결정: 최신 퀘스트 원천 데이터는 `json.tarkov.dev`를 사용하고, 외부 데이터 구조를 준현 헬퍼 내부 퀘스트 모델로 변환하여 사용한다.
- 이유: 최신 게임 데이터를 외부에서 갱신받되 제품 로직 전체가 특정 외부 API 스키마에 직접 종속되는 것을 피하기 위해서다.
- 대안/배경: 외부 API 원본 구조를 UI/비즈니스 로직 전반에서 직접 사용하는 방식은 채택하지 않는다.
- 영향: 향후 데이터 계층은 외부 원천 데이터와 준현 헬퍼의 제품 모델 사이에 변환 경계를 둬야 한다.
- 대체한 결정: 없음

## DEC-010 — 준현 헬퍼에서는 수주 가능 퀘스트를 이미 수락한 것으로 간주한다

- 상태: `CONFIRMED`
- 날짜: 2026-08-08
- 결정: 실제 게임의 `잠김 → 수주 가능 → 진행 중` 흐름 중 준현 헬퍼는 수주 가능한 퀘스트를 사용자가 모두 수락한다고 가정한다. 따라서 별도의 수락 조작이나 수락 여부 상태를 관리하지 않는다.
- 이유: 사용자는 실제로 수주 가능한 퀘스트를 모두 수락할 예정이며, 게임과 헬퍼에서 같은 수락 작업을 반복할 필요가 없다.
- 대안/배경: 준현 헬퍼에 별도의 `수락` 버튼과 진행 중 상태 전환을 두는 방식은 불필요한 이중 입력을 만든다.
- 영향: 준현 헬퍼의 핵심 퀘스트 상태는 `잠김 → 현재 퀘스트 → 완료`로 단순화된다. 현재 퀘스트는 해금 조건을 만족하고 아직 완료되지 않은 퀘스트다.
- 대체한 결정: 없음

## DEC-011 — 퀘스트 해금에 필요한 사용자 상태는 진행 프로필에서 관리한다

- 상태: `CONFIRMED`
- 날짜: 2026-08-08
- 결정: 사용자 레벨, 진영, 우호도, 에디션, 프레스티지 횟수, 완료 퀘스트 등 퀘스트 해금 판정에 필요한 캐릭터 상태를 해당 게임 진행 프로필에서 입력·수정할 수 있게 한다.
- 이유: 준현 헬퍼의 현재 퀘스트 목록이 실제 게임의 캐릭터 상태와 최대한 일치하려면 퀘스트 선행 조건을 판정할 사용자 컨텍스트가 필요하다.
- 대안/배경: 모든 퀘스트를 보여주고 사용자가 직접 골라내는 방식은 목표가 아니다.
- 영향: 실제 최신 퀘스트 데이터에서 새로운 선행 조건 유형이 확인되면 프로필과 판정 시스템을 확장한다. 프로필은 퀘스트 기능뿐 아니라 다른 기능에서도 공통 사용자 상태의 기준이 될 수 있다.
- 대체한 결정: 없음

## DEC-012 — 게임 모드별 캐릭터 진행 상태는 서로 독립된 프로필이다

- 상태: `CONFIRMED`
- 날짜: 2026-08-08
- 결정: PvP, PvE, 시즌 계열 등 서로 다른 게임 모드의 캐릭터는 하나의 프로필 안에서 모드 값을 바꾸는 방식이 아니라 서로 독립된 게임 진행 프로필로 관리한다.
- 이유: 실제 게임에서도 모드별 캐릭터는 레벨, 퀘스트 진행, 평판, 은신처 등 진행 상태가 서로 다른 별도 캐릭터다.
- 대안/배경: `게임 모드`를 하나의 프로필 필드로 두고 진행 상태를 공유하는 방식은 실제 게임 구조와 맞지 않는다.
- 영향: 퀘스트 완료 상태와 캐릭터 진행 정보는 선택된 게임 진행 프로필에 귀속한다. 다른 캐릭터 종속 기능도 같은 원칙을 우선 적용한다.
- 대체한 결정: DEC-011의 과거 `게임 모드`를 프로필 내부 조건값처럼 본 해석을 명확히 수정한다.

## DEC-013 — 퀘스트 해금 시간 지연은 준현 헬퍼에서 판정하지 않는다

- 상태: `CONFIRMED`
- 날짜: 2026-08-08
- 결정: `AvailableAfter`와 같이 선행 조건 충족 후 실제 시간이 지나야 퀘스트가 등장하는 지연 조건은 준현 헬퍼의 현재 퀘스트 판정에 사용하지 않는다.
- 이유: 준현 헬퍼는 게임에서 실제로 퀘스트가 완료된 시각을 자동으로 알 수 없고, 사용자가 완료 처리를 늦게 입력하면 계산된 해금 시각이 오히려 실제 게임과 어긋날 수 있다.
- 대안/배경: 사용자가 완료 시각을 별도로 입력하거나 준현 헬퍼에서 완료 버튼을 누른 시간을 기준으로 타이머를 시작하는 방식은 불필요한 입력 또는 잘못된 동기화를 만들 수 있어 채택하지 않는다.
- 영향: 시간 지연 외의 확인 가능한 선행 조건을 모두 만족한 미완료 퀘스트는 준현 헬퍼에서 현재 퀘스트로 간주한다. 이 때문에 시간 지연이 있는 일부 퀘스트는 실제 게임보다 일찍 표시될 수 있으며 이는 의도된 근사 동작이다.
- 대체한 결정: 없음

## DEC-014 — 퀘스트 UI의 시각적 형태는 제품 의도 확정 후 구현 단계에서 결정한다

- 상태: `CONFIRMED`
- 날짜: 2026-08-08
- 결정: 현재 제품 설계 단계에서는 퀘스트 목록과 상세 화면의 시각적 레이아웃을 확정하지 않는다. 먼저 어떤 퀘스트를 보여줘야 하는지와 어떤 정보/행동이 필요한지를 확정한다.
- 이유: 사용자는 현재 퀘스트 기능의 핵심 의도를 선행 조건 판정과 정보 제공에 두고 있으며, 시각적 표현은 구현의 영역으로 미루기를 원한다.
- 대안/배경: 초기 제품 설계 단계에서 카드/표/패널 등의 UI 형태까지 고정하는 방식은 보류한다.
- 영향: 향후 UI 설계는 확정된 기능 동작을 가장 잘 지원하는 형태로 개발자가 제안하고 검증한다.
- 대체한 결정: 없음

## DEC-015 — 게임 데이터 갱신은 GPT 없이 프로그램이 독립적으로 수행한다

- 상태: `CONFIRMED`
- 날짜: 2026-08-08
- 결정: GPT/개발자는 외부 API 데이터의 의미와 변환 규칙을 개발 시점에 설계·구현하고, 이후 일반적인 패치 데이터 갱신은 준현 헬퍼가 온라인 데이터를 직접 내려받아 동일한 규칙으로 내부 데이터베이스를 재구축하도록 한다.
- 이유: 패치마다 GPT가 새 데이터를 다시 해석하고 프로그램용 데이터로 수작업 변환해야 한다면 제품이 GPT와 개발자 작업에 지속적으로 종속되고 유지보수 비용이 커진다.
- 대안/배경: `새 데이터 → GPT 해석 → 프로그램에 새 데이터 전달` 방식을 채택하지 않는다.
- 영향: 런타임/일반 데이터 업데이트 경로에는 GPT 의존성이 없어야 한다. 변환 규칙은 프로그램 코드와 검증 가능한 내부 모델로 구현한다.
- 대체한 결정: DEC-009의 퀘스트 전용 변환 원칙을 제품 전체 데이터 아키텍처 원칙으로 확장한다.

## DEC-016 — 핵심 데이터 영역은 퀘스트·은신처·탄약이며 필요 아이템은 파생 데이터로 만든다

- 상태: `CONFIRMED`
- 날짜: 2026-08-08
- 결정: 현재 핵심 데이터베이스 업데이트 범위는 퀘스트, 은신처, 탄약으로 한다. 필요 아이템 목록은 별도 고정 데이터가 아니라 최신 퀘스트 요구 아이템과 은신처 업그레이드 요구 아이템에서 계산해 만든다.
- 이유: 사용자가 실제로 필요한 핵심 기능은 최신 데이터베이스 유지와 그 데이터에서 필요한 정보를 파생해 쓰는 것이다.
- 대안/배경: 필요 아이템을 패치마다 별도 수작업 데이터베이스로 관리하는 방식은 중복과 불일치를 만든다.
- 영향: 퀘스트/은신처 데이터가 갱신되면 필요 아이템도 같은 내부 데이터에서 다시 계산되어 자동으로 최신화되어야 한다. 지도는 아직 API 공급원을 확보하지 않아 이 확정 데이터 파이프라인에서 제외한다.
- 대체한 결정: 없음

## DEC-017 — 외부 API 스키마는 안정적일 수 있지만 불변으로 가정하지 않는다

- 상태: `CONFIRMED`
- 날짜: 2026-08-08
- 결정: 게임 데이터의 값과 내용 변경은 자동으로 흡수하되, 외부 API의 데이터 형식 자체가 영원히 동일하다고 가정하지 않는다. 데이터 갱신 전 스키마/필수 필드를 검증하고 비호환 변경을 감지해야 한다.
- 이유: API의 핵심 필드가 삭제되거나 타입·의미가 바뀐 상황에서 기존 변환기를 강제로 적용하면 조용히 잘못된 내부 데이터베이스를 만들 수 있다.
- 대안/배경: 모든 미래 API 응답이 현재 형식과 동일하다고 전제하고 무조건 파싱하는 방식은 채택하지 않는다.
- 영향: 호환 가능한 데이터 내용 변화는 자동 업데이트한다. 비호환 스키마 변화 시에는 갱신을 안전하게 중단하고 마지막 정상 데이터를 보호하며, 필요한 경우 변환 코드 자체를 업데이트한다.
- 대체한 결정: 없음

## DEC-018 — 준현 헬퍼 런타임은 추론하지 않고 명시적 규칙만 실행한다

- 상태: `CONFIRMED`
- 날짜: 2026-08-08
- 결정: 준현 헬퍼는 생각하는 AI나 휴리스틱 추론 시스템으로 동작하지 않는다. 개발 단계에서 검증해 구현한 명시적 규칙을 동일한 입력에 동일하게 적용하는 결정론적 도구로 만든다.
- 이유: 프로그램의 결과가 숨은 추론이나 상황별 추측에 의존하면 사용자가 결과를 신뢰하기 어렵고, 개발자도 오류 원인과 기능 관계를 파악하기 어려워 유지보수가 다시 복잡해진다.
- 대안/배경: 런타임 GPT 호출, 의미가 불명확한 새 API 값을 기존 값과 비슷하다는 이유로 자동 추정, 입력 누락을 임의의 기본값으로 채워 진행 판정을 계속하는 방식은 채택하지 않는다.
- 영향: 알려진 데이터와 규칙은 단순 계산으로 처리한다. 필요한 입력이 없거나 새로운 의미를 안전하게 해석할 수 없으면 `판정 불가` 또는 데이터 업데이트 실패로 명시적으로 처리하고 기존 정상 데이터를 보존한다. 새로운 규칙이 필요하면 개발 단계에서 의미를 검증한 뒤 코드와 테스트에 추가한다.
- 대체한 결정: 없음

## DEC-019 — 필요 아이템은 현재가 아니라 미래의 가능한 진행 전체를 계획한다

- 상태: `CONFIRMED`
- 날짜: 2026-08-08
- 결정: 필요 아이템의 기본 범위는 현재 진행 중인 퀘스트만이 아니라 현재 캐릭터가 앞으로 도달할 가능성이 남아 있는 퀘스트와, 명시된 현재 은신처 레벨 이후 모든 미래 업그레이드 재료를 포함한다. 완료됐거나 진영·에디션·게임 모드·확정된 진행 때문에 영구적으로 불가능하다고 증명되는 요구만 제외한다.
- 이유: 필요 아이템을 모으는 주된 목적은 앞으로의 퀘스트와 은신처 업그레이드에 대비해 물건을 미리 확보하는 것이기 때문이다.
- 대안/배경: 현재 Quest와 다음 Hideout 레벨만 합산하면 사용자가 나중에 필요한 물건을 미리 보관하는 핵심 목적을 달성하지 못한다.
- 영향: 레벨·상인·프레스티지처럼 미래에 충족 가능한 조건은 아직 잠겨 있어도 보관 계획에 남는다. 실패 분기나 미지원 조건처럼 미래 가능성을 확정하지 못하는 경우에는 잘못된 정리 안내를 피하기 위해 보수적으로 가능성에 남긴다.
- 대체한 결정: 없음

## DEC-020 — 사용자 보유량은 업데이트와 독립적으로 보존하고 초과분은 정리 필요로 지속 표시한다

- 상태: `SUPERSEDED IN PART BY DEC-025`
- 날짜: 2026-08-08
- 결정: 사용자가 입력한 아이템 보유량은 User Progress의 독립 사실로 보존하며, Game Content 업데이트나 퀘스트/은신처 요구량 변화로 자동 삭제·차감하지 않는다. 최신 미래 필요량보다 보유량이 많은 경우 안전하게 처분 가능한 초과분을 `정리 필요`로 표시하고, 사용자가 실제 보유량을 수정할 때까지 목록에 남긴다.
- 이유: 대형 업데이트 후 요구 아이템이 바뀌었는데 기존 필요 목록만 다시 계산하면, 사용자는 과거 정보를 믿고 모아둔 물건이 왜 필요 없어졌는지 알지 못한 채 계속 창고에 보관할 수 있다. 이 문제를 막는 것이 준현 헬퍼의 핵심 제작 목적 중 하나다.
- 대안/배경: 필요량이 0이 된 아이템을 목록에서 조용히 제거하거나, 업데이트와 함께 사용자 보유량을 삭제하는 방식은 채택하지 않는다.
- 영향: Game Content update로 보유량을 임의 삭제하지 않는 원칙은 유지한다. 다만 사용자가 명시적으로 Quest 완료/Hideout 업그레이드 조작을 한 경우 실제 소비된 고정 재료를 자동 차감하는 정책은 DEC-025가 대체 정의한다.
- 대체한 결정: Quest/Hideout 진행 조작 시 자동 차감 금지 부분만 DEC-025가 대체

## DEC-021 — 탄약은 원본 성능과 수급 사실을 비교하는 읽기 전용 기능으로 둔다

- 상태: `CONFIRMED`
- 날짜: 2026-08-08
- 결정: 탄약 기능은 선택된 게임 모드의 canonical Ammo 데이터를 읽기 전용으로 표시한다. 사용자는 구경과 이름으로 탄약을 찾고 원본 성능 수치와 상인 구매·물물교환·은신처 제작 수급처를 비교한다. 탄약 기능 자체의 별도 사용자 진행 상태나 자체 효율 점수는 만들지 않는다.
- 이유: 사용자의 목적은 최신 탄약 정보와 수급처를 빠르게 비교하는 것이며, 오래된 Tarkov-Helper의 임의 `방어구 효율` 같은 파생 휴리스틱은 게임 사실과 혼동될 수 있고 유지보수 부담을 만든다.
- 대안/배경: 탄약별 티어·추천 점수·방어구 등급 평가를 프로그램 자체 규칙으로 계산하거나, 탄약 기능을 프로필 진행 시스템에 불필요하게 결합하는 방식은 채택하지 않는다.
- 영향: 탄약 표는 damage/projectile count/penetration/armor damage/speed/fragmentation/accuracy/recoil 등의 canonical 필드와 확인 가능한 acquisition 조건을 표시한다. Game Content 갱신 시 새 데이터로 다시 표시하며 Quest/Hideout/Needed Items 사용자 사실은 수정하지 않는다.
- 대체한 결정: 없음

## DEC-022 — 퀘스트 실패와 분기는 확인 가능한 결과를 자동 계산하고 희귀 영구 실패만 수동 입력한다

- 상태: `CONFIRMED`
- 날짜: 2026-08-08
- 결정: 다른 퀘스트 완료로 확정되는 실패/분기 종료는 최신 Game Content의 `failConditions`와 사용자의 완료 퀘스트 사실에서 자동 계산한다. 아직 결과가 결정되지 않은 failed-only 분기는 정상적인 미래 가능성으로 유지하며 `Indeterminate`로 취급하지 않는다. 게임 데이터와 기존 사용자 사실로 알 수 없는 비재시작형 영구 실패에 한해서만 `실패 처리/실패 취소`를 사용자에게 제공한다.
- 이유: 최신 regular 퀘스트 510개와 prerequisite 607개를 조사한 결과 `Failed`가 포함된 선행 관계는 24개이고 failed-only는 4개뿐이며, taskStatus 실패 35건/23개 퀘스트는 다른 퀘스트 완료 사실만으로 자동 추론할 수 있다. 모든 퀘스트에 별도 분기 상태를 입력시키는 큰 시스템은 실제 문제 규모에 비해 복잡하다.
- 대안/배경: 모든 실패 상태를 사용자가 직접 관리하거나, failed-only 선행조건을 결과가 정해질 때까지 전부 `판정 문제`로 두는 방식은 채택하지 않는다. 재시작 가능한 레이드 실패를 영구 사용자 상태로 저장하는 방식도 채택하지 않는다.
- 영향: Quest 화면은 `진행 중 / 잠김 / 사용 불가 / 완료`를 정상 상태로 구분하고 `Indeterminate`는 실제 데이터/입력 문제에만 사용한다. 분기 결정 전에는 가능한 미래 요구 아이템을 모두 유지하고, 결정 후 불가능해진 분기의 요구량을 제거해 이미 모은 초과 재고를 `정리 필요`로 계산한다. explicit failure 기록은 패치가 삭제하지 않지만 새 canonical 규칙에서 더 이상 영구 실패 대상이 아니면 계산에 강제 적용하지 않는다.
- 대체한 결정: 없음

## DEC-023 — 사용자에게는 FIR 대신 인레이드/일반 용어를 사용한다

- 상태: `CONFIRMED`
- 날짜: 2026-08-09
- 결정: Item 보유량과 요구량에서 사용자에게 보이는 용어는 `FIR / Non-FIR` 대신 `인레이드 / 일반`을 사용한다. 저장 호환성을 위해 내부 모델·DB의 `Fir / NonFir` 식별자는 유지할 수 있다.
- 이유: 사용자가 Tarkov 플레이 중 실제로 이해하기 쉬운 한국어 표현으로 정보 구조를 단순화하기를 원한다.
- 대안/배경: 내부 영문 약어를 화면까지 그대로 노출하는 방식은 정보 밀도를 높이고 사용자 표현과 맞지 않는다.
- 영향: Quest 제출 Item, Hideout 재료, Needed Items 목록/상세, 유동 제출 진행 표시 등 사용자 UI는 `인레이드 / 일반` 용어를 사용한다. 내부 데이터 의미와 계산 규칙은 바뀌지 않는다.
- 대체한 결정: 없음

## DEC-024 — Ammo 비교 대상은 현재 healthy Wiki Ballistics membership으로 제한한다

- 상태: `CONFIRMED`
- 날짜: 2026-08-09
- 결정: Tarkov Wiki Ballistics source가 정상적으로 해석·매칭된 경우 Ammo 비교 화면에는 현재 Ballistics 표와 안전하게 매칭된 탄약만 표시한다. 영구 hard-coded allowlist는 만들지 않으며 raw Ammo 성능의 1차 원천은 계속 `json.tarkov.dev`로 둔다.
- 이유: 사용자는 장난식·미사용·실제 비교 가치가 없는 탄약과 그 구경이 Ammo 표에 섞이는 것을 원하지 않으며, Ballistics 표 등록 여부를 비교 대상의 실용적 기준으로 지정했다. 동시에 패치마다 수작업 목록을 갱신하면 제품의 self-updating 원칙과 충돌한다.
- 대안/배경: 모든 json.tarkov.dev Ammo를 무조건 표시하거나, 현재 탄약 이름을 코드에 고정 allowlist로 넣는 방식은 채택하지 않는다.
- 영향: healthy Wiki enrichment에서는 current table unique-match 여부가 Ammo 표와 caliber dropdown의 inclusion 기준이 된다. Wiki source가 unavailable/비정상이라 membership을 신뢰할 수 없을 때는 기본 Game Content를 삭제하거나 빈 화면을 만들지 않고 raw Ammo를 임시 표시하며 source 상태를 명시한다. Armor effectiveness 자체는 계속 Wiki의 명시된 0~6 값만 사용하고 자체 heuristic을 만들지 않는다.
- 대체한 결정: DEC-021의 read-only raw-stat 원칙을 유지하면서 화면의 비교 대상 범위를 추가로 정의한다.

## DEC-025 — Quest/Hideout 진행 조작은 고정 소모 아이템 보유량을 자동 조정한다

- 상태: `CONFIRMED`
- 날짜: 2026-08-09
- 결정: 사용자가 준현 헬퍼에서 Quest 완료 또는 Hideout 업그레이드를 명시적으로 처리하면 해당 진행에서 실제로 소모되는 고정 Item requirement를 tracked Inventory에서 자동 차감한다. 인레이드 필수 요구는 인레이드에서만, 일반 요구는 일반을 우선 사용하고 부족분만 인레이드에서 사용한다. 실제 자동 차감한 인레이드/일반 수량을 소비 ledger로 기록한다.
- 이유: 진행 상태를 완료/업그레이드했는데 이미 사용한 재료가 보유량에 계속 남으면 Needed Items와 cleanup 판단이 실제 게임 상태에서 어긋난다.
- 대안/배경: 모든 소비를 계속 수동으로만 수정하는 방식은 반복 입력을 만들며, requirement 수량 자체를 rollback 때 그대로 더하는 방식은 실제 당시 보유 부족량과 사용 타입을 복원하지 못한다.
- 영향: rollback 시 소비 ledger가 있으면 복원 여부를 묻는다. 복원하면 정확한 실제 차감량만 되돌리고 ledger를 제거한다. 복원하지 않으면 ledger를 유지해 같은 Quest 재완료/동일 Hideout level 재업그레이드에서 중복 차감을 방지한다. Game Content update 자체는 보유량을 차감하지 않는다.
- 대체한 결정: DEC-020의 `퀘스트 완료 시 자동 차감도 하지 않는다` 부분

## DEC-026 — 유동 제출의 실제 소비 Item은 자동 추정하지 않는다

- 상태: `CONFIRMED`
- 날짜: 2026-08-09
- 결정: 하나의 Quest objective가 여러 AcceptedItemIds 중 임의 조합을 허용하는 유동 제출인 경우 완료 처리 시 프로그램이 어느 후보 Item이 실제 제출됐는지 임의 선택해 Inventory를 차감하지 않는다.
- 이유: 준현 헬퍼가 가진 Quest 완료 사실과 후보별 보유량만으로는 사용자가 게임에서 실제 어떤 후보를 제출했는지 확정할 수 없다.
- 대안/배경: 보유량이 많은 후보부터 차감하거나 특정 정렬 순서의 후보를 자동 선택하는 휴리스틱은 실제 Inventory를 조용히 왜곡할 수 있어 채택하지 않는다.
- 영향: 고정 제출은 DEC-025에 따라 자동 처리하지만 flexible hand-in 후보의 실제 소비량은 사용자가 보유량을 조정한다. 이는 DEC-018의 비추론 원칙을 따른다.
- 대체한 결정: 없음

## DEC-027 — Wiki Ballistics membership과 방탄 효율 매칭은 별도 사실이다

- 상태: `CONFIRMED`
- 날짜: 2026-08-09
- 결정: Ammo가 현재 healthy Wiki Ballistics 표에 등록되어 있다는 사실과 Armor Class 1~6 effectiveness 여섯 값을 안전하게 파싱·매칭했다는 사실을 별도 canonical 필드로 관리한다.
- 이유: 표에는 존재하지만 effectiveness cell 파싱/매칭만 실패한 Ammo를 전체 비교 대상에서 제거하면 `12.7x108mm`처럼 실제 표에 존재하는 탄약이 사라질 수 있다.
- 대안/배경: `ArmorEffectiveness != null`을 곧 Wiki membership으로 간주하는 기존 3차 구현은 두 의미를 혼합했다.
- 영향: Content snapshot schema는 v3가 되며 `IsWikiBallisticsListed`와 optional `ArmorEffectiveness`를 별도로 저장한다. listed=true인 Ammo는 effectiveness가 `?`여도 비교 목록에 유지된다. source 전체가 unhealthy하면 membership을 추측하지 않는다.
- 대체한 결정: DEC-024의 membership 구현 의미를 명확히 보완

## DEC-028 — Prestige는 미입력 없이 0을 기본 사실로 취급한다

- 상태: `CONFIRMED`
- 날짜: 2026-08-09
- 결정: Profile의 Prestige는 사용자 제품 관점에서 nullable 입력이 아니라 0 이상의 정수이며 기본값은 0이다. 기존 payload의 null도 읽을 때 0으로 정규화한다.
- 이유: Prestige를 하지 않은 캐릭터의 정상 상태는 알 수 없음이 아니라 0이며, `미입력`은 불필요한 판정 불확실성과 조작을 만든다.
- 대안/배경: Prestige를 optional fact로 남겨 null이면 Quest를 Indeterminate로 두는 방식은 채택하지 않는다.
- 영향: 새 profile과 수정 UI 모두 0을 기본으로 사용하고 Prestige 요구 Quest는 0을 실제 알려진 값으로 평가한다.
- 대체한 결정: Profile에서 Prestige를 optional 입력으로 취급하던 과거 구현 의미

## DEC-029 — 제품 아이콘은 Game Content update 후 선다운로드한다

- 상태: `CONFIRMED`
- 날짜: 2026-08-09
- 결정: Quest 제출 후보, Hideout 재료, Ammo Item, Hideout station 등 현재 제품 화면에서 사용하는 이미지는 Game Content update가 성공한 뒤 로컬 image cache에 선다운로드한다. 동일 Item ID는 화면별로 별도 파일을 만들지 않고 공통 cache key를 사용한다.
- 이유: 화면 진입 시점의 lazy download에만 의존하면 아이콘이 늦게 나타나거나 사용자 체감상 적용되지 않는 문제가 반복된다.
- 대안/배경: 각 page가 표시될 때마다 독립 cache key로 네트워크 요청하는 방식은 중복과 지연을 만든다.
- 영향: update progress 후반에 image prefetch stage가 추가된다. 개별 image 실패는 권위 Game Content 실패가 아니므로 update 전체를 실패시키지 않는다.
- 대체한 결정: 기존 lazy-cache 정책을 보완
