# STATE — 현재 프로젝트 상태

> 이 문서는 새 개발자가 가장 빠르게 현재 상태를 복구하기 위한 핸드오프 문서입니다.

## 현재 Phase

**Phase 1 — 제품 발견(Product Discovery) 및 큰 틀 설계**

상태: `IN PROGRESS`

제품 코드는 아직 작성하지 않습니다.

## 프로젝트 이름

**준현 헬퍼**

저장소: `Propeex/JunhyunHelper`

## 프로젝트의 현재 핵심 정의

준현 헬퍼의 핵심은 **최신 Tarkov 데이터를 온라인에서 받아 프로그램 스스로 내부 데이터베이스로 변환·재구축하고, 그 데이터를 게임 진행 상태와 결합해 사용하는 것**입니다.

가장 중요한 유지보수 원칙:

`새 패치 데이터 → GPT가 다시 해석 → 수작업 데이터 갱신`

이 구조를 만들지 않습니다.

목표 구조:

`GPT/개발자가 변환 규칙 구현 → 프로그램이 최신 데이터 다운로드 → 검증 → 변환 → DB 재구축 → 기능이 최신 DB 사용`

일반적인 데이터 업데이트에는 GPT가 필요하지 않아야 합니다.

## 현재 핵심 기능 범위

### 1. API 기반 데이터베이스 업데이트

`CONFIRMED`

현재 핵심 데이터 영역:

- 퀘스트
- 은신처
- 탄약

퀘스트 데이터는 `json.tarkov.dev`를 원천으로 사용하기로 확정했습니다.
은신처와 탄약의 정확한 공급원/엔드포인트는 구현 전 최신 상태를 검증합니다.

지도는 아직 적절한 API 공급원을 확보하지 않아 현재 확정 데이터 파이프라인 밖입니다.

### 2. 퀘스트

`CONFIRMED / 큰 틀 확보`

- 게임 모드별 캐릭터는 서로 독립된 진행 프로필입니다.
- 현재 프로필이 선행 조건을 만족한 미완료 퀘스트를 계산해 보여줍니다.
- 수주 가능한 퀘스트는 준현 헬퍼에서 이미 수락된 것으로 간주합니다.
- 사용자가 완료 처리하면 후속 퀘스트 판정을 갱신합니다.
- 장소/상인 기준 분류와 상세 정보/Wiki 접근을 지원합니다.
- 시간 지연 해금은 판정에서 제외합니다.
- 시각적 UI와 세부 예외 처리는 후속 설계로 미룹니다.

### 3. 은신처

`CONFIRMED / 큰 틀`

- 최신 은신처 데이터를 내부 DB로 변환해 사용합니다.
- 사용자 진행 상태와 결합합니다.
- 은신처 업그레이드 요구 아이템은 필요 아이템 계산의 원천이 됩니다.
- 세부 입력 방식과 UI는 아직 정하지 않습니다.

### 4. 탄약

`CONFIRMED / 큰 틀`

- 최신 탄약 데이터를 내부 DB로 변환해 사용합니다.
- 구경별 탄약과 정보/수급처 등을 제공하는 것이 핵심입니다.
- 세부 표시 방식은 아직 정하지 않습니다.

### 5. 필요 아이템

`CONFIRMED / 큰 틀`

별도 수작업 고정 데이터가 아닙니다.

`최신 퀘스트 요구 아이템 + 최신 은신처 업그레이드 요구 아이템 → 필요 아이템 목록`

퀘스트/은신처 데이터가 패치로 바뀌면 필요 아이템도 같은 데이터에서 다시 계산되어 최신화되어야 합니다.

## 데이터 아키텍처 핵심 원칙

`CONFIRMED`

기본 흐름:

`외부 API → 다운로드 → 스키마/필수 필드 검증 → 내부 모델 변환 → 로컬 DB 구축/갱신 → 기능 사용`

- 제품 로직/UI는 외부 원본 JSON 스키마를 직접 사용하지 않습니다.
- 퀘스트/은신처/탄약마다 변환 계층을 둡니다.
- 데이터 값/내용 변화는 자동 업데이트 대상으로 봅니다.
- 외부 API 형식이 영원히 고정된다고 가정하지 않습니다.
- 비호환 스키마 변경은 감지하고 잘못된 새 DB로 덮어쓰지 않습니다.
- 가능한 경우 마지막 정상 DB를 유지합니다.

## 현재 확정된 핵심 프로젝트 원칙

- 기존 `Propeex/Tarkov-Helper`는 참고 자료일 뿐 정답/사양이 아닙니다.
- 사용자는 제품 의도와 피드백에 집중하고 개발 절차는 개발자가 책임집니다.
- 구두 의도는 개발자 해석 → 사용자 교정 → 합의 후 공식 요구사항으로 기록합니다.
- 현재는 세부 UX보다 전체 핵심 기능과 데이터 관계를 먼저 확정합니다.
- 중요한 결정과 현재 상태는 GitHub 공식 문서에 즉시 반영합니다.

## 후속/별도 범위

- 지도: 기능 의도는 유지하지만 API 공급원 미확정
- RatScanner: 후속 통합 대상

## 지금 미루는 세부사항

- 구체적인 UI 레이아웃
- 완료 취소/오입력 복구
- 실패/분기형 퀘스트의 세부 동기화
- 정확한 캐시/오프라인 정책
- DB 엔진/프레임워크/언어
- 자동 갱신 주기
- 상세 프로필 입력 화면

## 바로 다음 행동

1. 현재 정리된 핵심 제품/데이터 구조를 기준으로 은신처와 탄약 데이터 공급원이 실제 필요한 정보를 제공하는지 검증합니다.
2. 퀘스트 + 은신처에서 필요 아이템을 파생할 수 있는 데이터 관계를 검증합니다.
3. 핵심 기능 전체의 큰 틀이 충분히 확인되면 세부 구현 설계로 내려갑니다.
4. 기술 스택과 저장 구조는 그때 결정합니다.
5. 구현 시 데이터 변환기와 스키마 검증을 핵심 테스트 대상으로 둡니다.

## 마지막 갱신

2026-08-08 — 제품의 최상위 원리를 `GPT에 반복 의존하지 않는 데이터 기반 구조`로 확정. 퀘스트·은신처·탄약을 API 기반 핵심 DB 영역으로, 필요 아이템을 퀘스트+은신처의 파생 데이터로 정의. 외부 API 스키마는 불변으로 가정하지 않고 검증/안전 실패 원칙을 채택.
